{
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-bench": "^29.7.1",
    "@beezydev/create-files-from-template-cli": "^1.8.0"
  },
  "scripts": {
    "test": "jest --watchAll",
    "bench": "jest --projects jest-bench.config.json",
    "new": "cfft -n"
  }
}
